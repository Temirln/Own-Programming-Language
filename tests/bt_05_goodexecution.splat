program
	
	fact(n : Integer) : Integer is 
	result : Integer;
	begin
		if ((n == 0) or (n == 1)) then 
			result := 1;
		else
			result := (fact((n-1)) * n);
		end if;
		return result;
	end;

	num : Integer;

begin
	
	num := 4;
	print fact(num);
end;
