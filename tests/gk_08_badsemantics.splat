program
	k: Integer;
	donothing(x : Integer) : Integer is
	y: Integer;
	begin
	 	x := (x + 1);
		return x;
	end ;
	donothing2(x : Integer) : Integer is
	begin
		x := (x + 1);
		return x;
	end ;
begin
	print "Howdy!";
	l := donothing(3);
		
end;
