program

	donothing(w : Boolean, x : Integer) : Integer is
		x : Integer;
	begin
		x := (x + 1);
		return x;
	end ;
	
begin
	z := donothing((99 + "uh oh!"));
	print ("Look at the rule for print");
end;